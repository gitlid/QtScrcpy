find_package(QT NAMES Qt6 Qt5 REQUIRED COMPONENTS Widgets)
find_package(Qt${QT_VERSION_MAJOR} REQUIRED COMPONENTS Widgets Network)
set(CMAKE_AUTOMOC ON)
set(CMAKE_CXX_STANDARD 11)
set(CORE ${CMAKE_CURRENT_LIST_DIR}/../QtScrcpy/QtScrcpyCore)
add_executable(actionmacro_integration actionmacro_integration.cpp
    ../QtScrcpy/ui/actionmacrodialog.cpp ../QtScrcpy/ui/actionmacrodialog.h
    ../QtScrcpy/ui/actionmacrohotkey.h ../QtScrcpy/ui/keymapeditor.cpp ../QtScrcpy/ui/keymapeditor.h)
target_include_directories(actionmacro_integration PRIVATE ${CORE}/src/common ${CORE}/src/device/android ${CORE}/src/device/controller ${CORE}/src/device/controller/inputconvert)
target_link_libraries(actionmacro_integration PRIVATE QtScrcpyCore Qt${QT_VERSION_MAJOR}::Widgets Qt${QT_VERSION_MAJOR}::Network)
foreach(case_name IN ITEMS video_required stop_clears_queue transport_failure playback_guards disconnected_dialog)
    add_test(NAME integration_${case_name} COMMAND actionmacro_integration ${case_name})
    set_tests_properties(integration_${case_name} PROPERTIES TIMEOUT 15)
endforeach()
add_executable(uhid_integration uhid_integration.cpp)
target_include_directories(uhid_integration PRIVATE ${CORE}/src/common ${CORE}/src/device/android ${CORE}/src/device/controller ${CORE}/src/device/controller/inputconvert ${CORE}/src/device/controller/receiver)
target_link_libraries(uhid_integration PRIVATE QtScrcpyCore Qt${QT_VERSION_MAJOR}::Widgets Qt${QT_VERSION_MAJOR}::Network)
foreach(case_name IN ITEMS wire mapping rollover schema feedback lifecycle macro emergency game_mapping)
    add_test(NAME uhid_${case_name} COMMAND uhid_integration ${case_name})
    set_tests_properties(uhid_${case_name} PROPERTIES TIMEOUT 15)
endforeach()
add_executable(keymap_macro_v2_tests keymap_macro_v2_test.cpp ../QtScrcpy/ui/keymapeditor.cpp ../QtScrcpy/ui/keymapeditor.h)
target_include_directories(keymap_macro_v2_tests PRIVATE ../QtScrcpy/ui)
target_link_libraries(keymap_macro_v2_tests PRIVATE QtScrcpyCore Qt${QT_VERSION_MAJOR}::Widgets Qt${QT_VERSION_MAJOR}::Network)
foreach(case_name IN ITEMS settings_eight settings_invalid settings_roundtrip keymap_roundtrip keymap_conflicts keymap_bounds keymap_transaction editor_drag pause_shortcut)
    add_test(NAME v2ui_${case_name} COMMAND keymap_macro_v2_tests ${case_name})
    set_tests_properties(v2ui_${case_name} PROPERTIES TIMEOUT 15)
endforeach()
add_executable(macroexecution_integration ${CORE}/tests/macroexecution_test.cpp)
target_include_directories(macroexecution_integration PRIVATE ${CORE}/src/common ${CORE}/src/device/android ${CORE}/src/device/controller ${CORE}/src/device/controller/inputconvert)
target_link_libraries(macroexecution_integration PRIVATE QtScrcpyCore Qt${QT_VERSION_MAJOR}::Widgets Qt${QT_VERSION_MAJOR}::Network)
foreach(case_name IN ITEMS speed_bounds speed_eight pause_wait pause_interval time_limit limit_during_interval limit_excludes_pause stop_paused touch_boundary key_boundary hid_pause_release recording_pause record_stop_paused geometry_paused app_geometry app_resume_wait app_stop_geometry pause_reentrancy busy_paused same_timestamp)
    add_test(NAME v2execution_${case_name} COMMAND macroexecution_integration ${case_name})
    set_tests_properties(v2execution_${case_name} PROPERTIES TIMEOUT 15)
endforeach()
if(QT_VERSION_MAJOR EQUAL 6)
    find_package(Qt6 REQUIRED COMPONENTS OpenGLWidgets)
    target_link_libraries(actionmacro_integration PRIVATE Qt6::OpenGLWidgets)
endif()
if(MSVC)
    target_compile_options(uhid_integration PRIVATE /utf-8 /W3 /WX)
    target_compile_options(actionmacro_integration PRIVATE /utf-8)
    target_compile_options(keymap_macro_v2_tests PRIVATE /utf-8)
    target_compile_options(macroexecution_integration PRIVATE /utf-8)
endif()

# Real Qt mouse events and fake Controller transport; no phone required.
add_executable(mousebinding_ui_tests mousebinding_ui_test.cpp
    ../QtScrcpy/ui/keymapeditor.cpp ../QtScrcpy/ui/keymapeditor.h)
target_include_directories(mousebinding_ui_tests PRIVATE ../QtScrcpy/ui)
target_link_libraries(mousebinding_ui_tests PRIVATE Qt${QT_VERSION_MAJOR}::Widgets)
foreach(case_name IN ITEMS menu direct left_focus keyboard alias_conflict invalid namespaces editor_roundtrip toggle_routing)
    add_test(NAME mouseui_${case_name} COMMAND mousebinding_ui_tests ${case_name})
    set_tests_properties(mouseui_${case_name} PROPERTIES TIMEOUT 15)
endforeach()
add_executable(mousemapping_tests ${CORE}/tests/mousemapping_test.cpp)
target_include_directories(mousemapping_tests PRIVATE ${CORE}/src/common ${CORE}/src/device/android
    ${CORE}/src/device/controller ${CORE}/src/device/controller/inputconvert)
target_link_libraries(mousemapping_tests PRIVATE QtScrcpyCore Qt${QT_VERSION_MAJOR}::Widgets Qt${QT_VERSION_MAJOR}::Network)
foreach(case_name IN ITEMS left right middle back forward aliases drag multi twice android joystick namespaces move_no_leak toggle simultaneous macro pause toggle_cancels_delayed uhid_priority)
    add_test(NAME mouse_${case_name} COMMAND mousemapping_tests ${case_name})
    set_tests_properties(mouse_${case_name} PROPERTIES TIMEOUT 15)
endforeach()
if(MSVC)
    target_compile_options(mousebinding_ui_tests PRIVATE /utf-8 /W3 /WX)
    target_compile_options(mousemapping_tests PRIVATE /utf-8 /W3 /WX)
endif()

add_executable(inputrouting_tests ${CORE}/tests/inputrouting_test.cpp)
target_include_directories(inputrouting_tests PRIVATE ${CORE}/src/common ${CORE}/src/device/android
    ${CORE}/src/device/controller ${CORE}/src/device/controller/inputconvert ${CORE}/src/device/controller/inputconvert/keymap)
target_link_libraries(inputrouting_tests PRIVATE QtScrcpyCore Qt${QT_VERSION_MAJOR}::Widgets Qt${QT_VERSION_MAJOR}::Network)
foreach(case_name IN ITEMS legacy_look_on look_disabled look_enabled ordinary_no_capture legacy_capture unmapped_custom unmapped_normal mapped_target normal_primary_only look_moves released_side_mapped release_flag_no_view replace_capture edit_capture reload_no_view invalid_look_flags invalid_vertical_ratio unknown_with_primary app_profile app_profile_busy)
    add_test(NAME routing_${case_name} COMMAND inputrouting_tests ${case_name})
    set_tests_properties(routing_${case_name} PROPERTIES TIMEOUT 15)
endforeach()
foreach(case_name IN ITEMS look_default look_roundtrip look_validation cursor_scope cursor_restore cursor_deleted)
    add_test(NAME mouseui_${case_name} COMMAND mousebinding_ui_tests ${case_name})
    set_tests_properties(mouseui_${case_name} PROPERTIES TIMEOUT 15)
endforeach()
if(MSVC)
    target_compile_options(inputrouting_tests PRIVATE /utf-8 /W3 /WX)
endif()

add_executable(hybridkeyboard_tests ${CORE}/tests/hybridkeyboard_test.cpp)
target_include_directories(hybridkeyboard_tests PRIVATE ${CORE}/src/common ${CORE}/src/device/android ${CORE}/src/device/controller ${CORE}/src/device/controller/inputconvert)
target_link_libraries(hybridkeyboard_tests PRIVATE QtScrcpyCore Qt${QT_VERSION_MAJOR}::Widgets Qt${QT_VERSION_MAJOR}::Network)
if(MSVC)
    target_compile_options(hybridkeyboard_tests PRIVATE /utf-8 /W3 /WX)
endif()
foreach(case_name IN ITEMS unmapped_letters tab escape mapped_priority mouse_only chord concurrent toggle_release mouse_toggle repeat orphan_release focus_release physical_pair toggle_reserved mapped_modifier legacy_mode reload mixed_macro paused_macro)
    add_test(NAME hybrid_${case_name} COMMAND hybridkeyboard_tests ${case_name})
    set_tests_properties(hybrid_${case_name} PROPERTIES TIMEOUT 15)
endforeach()

add_executable(keyboardrouting_policy_test ${CORE}/tests/keyboardrouting_policy_test.cpp)
target_include_directories(keyboardrouting_policy_test PRIVATE ${CORE}/src/device/controller)
add_test(NAME keyboardrouting_policy COMMAND keyboardrouting_policy_test)

add_executable(doubleclick_tests ${CORE}/tests/doubleclick_test.cpp)
target_include_directories(doubleclick_tests PRIVATE ${CORE}/src/common ${CORE}/src/device/android
    ${CORE}/src/device/controller ${CORE}/src/device/controller/inputconvert)
target_link_libraries(doubleclick_tests PRIVATE QtScrcpyCore Qt${QT_VERSION_MAJOR}::Widgets Qt${QT_VERSION_MAJOR}::Network)
if(MSVC)
    target_compile_options(doubleclick_tests PRIVATE /utf-8 /W3 /WX)
endif()
foreach(case_name IN ITEMS plain disabled unbound mapped scaled second_drag burst other_buttons uhid macro stop_release invalid_geometry)
    add_test(NAME doubleclick_${case_name} COMMAND doubleclick_tests ${case_name})
    set_tests_properties(doubleclick_${case_name} PROPERTIES TIMEOUT 15)
endforeach()
